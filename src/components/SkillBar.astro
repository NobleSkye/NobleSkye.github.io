---
interface Props {
  title: string;
  skills: Array<{
    name: string;
    percentage: number;
  }>;
}

const { title, skills } = Astro.props;
---

<div class="mb-8">
  <h2 class="text-gray-200 text-xl font-semibold mb-6">{title}</h2>
  <div class="space-y-4">
    {skills.map((skill) => (
      <div>
        <div class="flex justify-between items-center mb-2">
          <span class="text-gray-300 font-medium">{skill.name}</span>
          <span class="text-gray-500 text-sm">{skill.percentage}%</span>
        </div>
        <div class="w-full bg-dark-lighter rounded-full h-2">
          <div 
            class="bg-gradient-to-r from-sky-400 to-blue-500 h-2 rounded-full transition-all duration-1000 ease-out"
            style={`width: ${skill.percentage}%`}
          ></div>
        </div>
      </div>
    ))}
  </div>
</div>
