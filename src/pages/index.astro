---
import Layout from '../layouts/Layout.astro';
import ProfileCard from '../components/ProfileCard.astro';
import Section from '../components/Section.astro';
import SkillBar from '../components/SkillBar.astro';

const educationData = [
  {
    year: "2022-2026",
    institution: "Vetreans Tribute CTA",
    degree: "Highschool Diploma",
    description: "Studied various subjects including Cyber Security, Mathematics, Science, English, and History."
  }
];

const experienceData = [
  {
    year: "2024-Current",
    institution: "",
    degree: "Minecraft Network Owner",
    description: "I am hosting my own Minecraft network called SkyeNet. Custom miniames & gamemodes on custom infrastructure. "
  }
];

const designSkills = [
  { name: "Web Design", percentage: 85 },
  { name: "Post Design", percentage: 80 },
];

const codingSkills = [
  { name: "Javascript", percentage: 80 },
  { name: "HTML/CSS", percentage: 95 },
  { name: "Java", percentage: 95 }
];
---

<Layout title="Skylar Steuber - Developer Portfolio">
  <main id="top" class="min-h-screen relative">
    <div class="relative z-10 min-h-screen flex items-center justify-center p-4">
      <div class="w-full max-w-7xl mx-auto">
        <div class="grid grid-cols-1 lg:grid-cols-3 gap-8 items-start">
          
          <!-- Profile Card -->
          <div id="profile" class="lg:col-span-1" data-scramble="profile">
            <ProfileCard 
              name="Skylar Steuber"
              title="Developer"
              image="profile-image.jpeg"
              skyenetUrl="https://skyemc.net"
              blueskyUrl="https://bsky.app/profile/nobleskye.dev"
              githubUrl="https://github.com/NobleSkye"
              resumeUrl="/file/Skylar Steuber's Resume.pdf"
              
            />
          </div>

          <!-- Main Content -->
          <div id="resume" class="lg:col-span-2 bg-dark rounded-3xl p-8" data-scramble="resume">
            <div class="grid grid-cols-1 lg:grid-cols-2 gap-8">
              
              <!-- Left Column -->
              <div>
                <!-- Resume Header -->
                <div class="mb-8">
                  <h1 class="text-white text-3xl font-bold mb-2">
                    Resume<span class="text-primary">.</span>
                  </h1>
                </div>

                <!-- Education -->
                <div id="education">
                  <Section title="Education" items={educationData} />
                </div>

                <!-- Experience -->
                <Section title="Experience" items={experienceData} />
              </div>

              <!-- Right Column -->
              <div class="space-y-8">
                <!-- Design Skills -->
                <div data-scramble="design">
                  <SkillBar title="Design Skills" skills={designSkills} />
                </div>

                <!-- Coding Skills -->
                <div data-scramble="coding">
                  <SkillBar title="Coding Skills" skills={codingSkills} />
                </div>
              </div>

            </div>
          </div>

        </div>
      </div>
    </div>

    <script>
      // @ts-nocheck
      function scrollToHash() {
        const raw = window.location.hash || '';
        const id = raw.startsWith('#') ? raw.slice(1) : raw;
        if (!id) return;

        const el = document.getElementById(decodeURIComponent(id));
        if (!el) return;

        // Let the browser finish layout first.
        requestAnimationFrame(() => {
          el.scrollIntoView({ behavior: 'smooth', block: 'start' });
        });
      }

      // Make hash navigation reliable on initial load + manual hash changes.
      window.addEventListener('hashchange', scrollToHash);
      window.addEventListener('load', () => setTimeout(scrollToHash, 0));
    </script>

  </main>
</Layout>

<style>
  /* Custom animations */
  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .animate-fade-in-up {
    animation: fadeInUp 0.8s ease-out forwards;
  }
</style>
